/** apat.js — Detall d'àpat (Fase D). Placeholder inicial. */
export function renderApat(id) {
  return `
    <section class="view-placeholder">
      <i data-lucide="moon" aria-hidden="true"></i>
      <h1>Àpat · ${id || ""}</h1>
      <p>Vista en construcció (Fase D).</p>
    </section>
  `;
}
