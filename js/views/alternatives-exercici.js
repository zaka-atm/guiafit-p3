/** alternatives-exercici.js — 3 alternatives IA (Fase C). Placeholder inicial. */
export function renderAlternativesExercici(exerciseId) {
  return `
    <section class="view-placeholder">
      <i data-lucide="sparkles" aria-hidden="true"></i>
      <h1>Alternatives IA · ${exerciseId || ""}</h1>
      <p>Vista en construcció (Fase C).</p>
    </section>
  `;
}
