/** recepta.js — Detall de recepta (Fase D). Placeholder inicial. */
export function renderRecepta(recipeId) {
  return `
    <section class="view-placeholder">
      <i data-lucide="chef-hat" aria-hidden="true"></i>
      <h1>Recepta · ${recipeId || ""}</h1>
      <p>Vista en construcció (Fase D).</p>
    </section>
  `;
}
