/** menu.js — Vista "Menú d'avui" (Fase D). Placeholder inicial. */
export function renderMenu() {
  return `
    <section class="view-placeholder">
      <i data-lucide="utensils-crossed" aria-hidden="true"></i>
      <h1>Menú d'avui</h1>
      <p>Vista en construcció (Fase D).</p>
    </section>
  `;
}
