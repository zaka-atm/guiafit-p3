/** exercici.js — Detall d'exercici (Fase C). Placeholder inicial. */
export function renderExercici(id) {
  return `
    <section class="view-placeholder">
      <i data-lucide="activity" aria-hidden="true"></i>
      <h1>Exercici · ${id || ""}</h1>
      <p>Vista en construcció (Fase C).</p>
    </section>
  `;
}
