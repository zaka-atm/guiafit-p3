/** alternatives-recepta.js — 3 receptes alternatives IA (Fase D). Placeholder inicial. */
export function renderAlternativesRecepta(apatId) {
  return `
    <section class="view-placeholder">
      <i data-lucide="sparkles" aria-hidden="true"></i>
      <h1>Receptes alternatives · ${apatId || ""}</h1>
      <p>Vista en construcció (Fase D).</p>
    </section>
  `;
}
